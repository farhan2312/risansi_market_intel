# AUDIT-FINDINGS.md

Read-only audit of risansi_market_intel at commit a2bc561. Findings from 4 workflow waves (23 area auditors, adversarial verification of every CRITICAL/HIGH) plus a hand audit of the infra layer and the four already-known items. CRITICAL/HIGH tagged **verified** survived an adversarial refutation pass; 3 HIGH candidates were refuted and excluded.

Totals after dedupe: **176** — CRITICAL 1 / HIGH 11 / MEDIUM 72 / LOW 92

| # | Sev | Conf | Lens | Location | What | Why it matters | Fix | Eff |
|---|-----|------|------|----------|------|----------------|-----|-----|
| 1 | CRITICAL | verified | CORRECTNESS | app/actions/risansi-revenue.ts:157 | deleteUpload(logId) removes revenue rows by (month, uploader) rather than by the upload it is undoing. There is no upload_id column on client_revenue_monthly, unlike client_pumps which does carry upload_id. | Confirmed live: mona.rathour@risansi.com has two revenue_upload_log rows for month 2026-06-30. Deleting either log wipes the revenue rows be | Add an upload_id column to client_revenue_monthly, stamp it on insert in uploadRevenue, and change deleteUpload to 'DELETE FROM client_revenue_monthly WHERE upload_id = $1' — the p | M |
| 2 | HIGH | verified | SECURITY | app/actions/risansi-tasks.ts:163 | deleteTask has only a session check (requireEmail returns an email, nothing more). No canViewClient, no created_by/assignee check on the task's id. | Any authenticated user — the lowest-privilege rep — can permanently delete any task in the portal by guessing an integer id. Tasks are the A | Load the task first (SELECT client_id, created_by, assigned_to_rep FROM tasks WHERE id=$1) and require hasRole(user.role,'admin') OR created_by=user.email OR assigned_to_rep=user.i | S |
| 3 | HIGH | verified | SECURITY | app/actions/risansi-tasks.ts:150 | updateTaskStatus only requires a session, then flips status/completed_by/completed_at on any task id with no client-scope or ownership check. Called from ActionQueueRow.tsx:39, ActivitiesTab.tsx:46, ClientActiv | Any authenticated user can mark another team's tasks complete or reopen them, tampering with action-registry records outside their tour. | Resolve the task's client_id and gate on canViewClient / assignee / creator / admin before the UPDATE, matching addTask. | S |
| 4 | HIGH | CONFIRMED | security | app/actions/risansi.ts:1432 | listSalesOrders (line 1432) and listPurchaseOrders (line 1507) are exported 'use server' actions with no session check and no visibility check at all. | Server actions compile to POST endpoints whose action IDs ship in the client bundle. Anyone (even unauthenticated) who replays the action ID | At the top of both functions: 'const user = await requireSession();' then fetch the opp's rep_id/client_id and 'if (!(await userCanEditOpp(user, rep_id, client_id))) throw new Erro | S |
| 5 | HIGH | CONFIRMED | security | app/actions/risansi.ts:317 | IDOR across contact PII: addContact/updateContact/deleteContact only call requireSession — no canViewClient — and updateContact's UPDATE (line 305 'WHERE id = $8') and deleteContact's DELETE are not scoped by c | Any authenticated rep can create, rewrite or delete any contact (names, phones, emails, WhatsApp) on any client in the company, including cl | In all three actions resolve the contact's real client_id server-side (SELECT client_id FROM contacts WHERE id=$1), require 'await canViewClient(await getCurrentUser(), clientId)', | S |
| 6 | HIGH | CONFIRMED | security | app/actions/risansi.ts:1577 | deleteOpportunity has no stage guard: a Won or Lost opportunity — which every edit path explicitly locks (lines 1140, 1265) — can be hard-deleted by any rep on the client's tour, and ON DELETE CASCADE (migratio | Booked revenue records (Order-in-Hand is computed from these SOs) can be irreversibly destroyed by a single click; the edit lock on Won/Lost | Fetch stage in the existing SELECT and throw for 'Won'/'Lost' unless hasRole(user.role,'admin'); longer-term add a deleted_at soft-delete column like clients have (setEndClient fil | S |
| 7 | HIGH | verified | security | app/api/debug-db/route.ts:4 | An unauthenticated debug endpoint returns live production data: row counts for clients/competitor_installed_base/orders, five real client codes + legal names + statuses, and the database name. proxy.ts matcher  | Anonymous disclosure of customer identities and business volume from an internet-facing B2B portal; also a free reconnaissance oracle for sc | Delete the route. If a health check is genuinely needed, keep only 'SELECT 1', return no row data, and gate it behind getServerSession + role==='sysadmin' (or a shared secret heade | S |
| 8 | HIGH | verified | SECURITY | app/api/risansi/opportunities/[id]/quotation/route.ts:49 | Upload passes if EITHER the mime looks like PDF OR the filename ends .pdf, so a file named x.pdf with type text/html (or image/svg+xml) is stored with that mime. GET line 130 echoes it as ''Content-Type': rec.m | Any rep who can edit an opportunity plants a page that executes in the browser of every colleague, including admins, who opens the quotation | Require BOTH conditions, verify the bytes start with '%PDF-', and on GET hardcode ''Content-Type': 'application/pdf'' plus ''X-Content-Type-Options': 'nosniff'' instead of echoing  | S |
| 9 | HIGH | verified | SECURITY | app/api/risansi/validate-revenue-codes/route.ts:4 | No session check of any kind (the file imports only NextResponse and risansiPool), and proxy.ts matches only /risansi/:path* and /admin/:path*, so /api/risansi/** has no edge gate. Anyone can POST a code list a | Unauthenticated enumeration of the client master (2,676 live rows). Codes are short and guessable (LEAD_*, 5-char codes) and the codes array | Add 'const user = await getCurrentUser(); if (!user.email) return 401;' plus 'hasRole(user.role,'admin')' (all three callers are admin upload boxes), and cap codes.length at 500 to | S |
| 10 | HIGH | verified | security | lib/db-risansi.ts:16 | TLS certificate verification is disabled on the only Postgres connection in the app. | The connection is encrypted but unauthenticated: anyone able to intercept the route to the remote DB host can present any certificate and re | Ship the provider CA and use 'ssl: { ca: process.env.DB_CA_CERT, rejectUnauthorized: true }'; keep the current setting only behind an explicit 'DB_SSL_INSECURE=1' dev flag. | S |
| 11 | HIGH | verified | CORRECTNESS | app/actions/risansi-visits.ts:114 | Toggling the visit form's Expansion question to "No" hard-deletes the linked opportunity selected only by visit_id + auto_source='expansion_plan', with no stage guard. opportunity_sales_orders FKs it ON DELETE  | auto_source survives stage promotion: 2 expansion_plan opportunities are currently at stage 'Quoted' and 1 at 'On Hold'. A rep re-opening th | Before deleting, read the stage; if it is past the initial stage, or auto_created is no longer true, or any opportunity_sales_orders row exists, detach instead — set visit_id/auto_ | M |
| 12 | HIGH | CONFIRMED | correctness | app/actions/risansi.ts:1342 | updateOpportunity writes NULL for every candidate column the form omits, and only rep_id/secondary_rep_id/drop_reason/negotiation_notes/probability are protected (lines 1361-1374). OppCompletionModal sends only | Routine deal closure erases the quotation reference, quote date and project name from every closed opportunity — exactly the fields the Exce | Apply the same omitted-field preserve guard to the rest: 'for (const k of Object.keys(candidates)) if (formData.get(k) === null) delete candidates[k];' (fields the form intends to  | M |
| 13 | MEDIUM | CONFIRMED | SIMPLIFICATIO | app/actions/risansi-admin-revenue.ts:32 | Two server actions named uploadRevenue write the same client_revenue_monthly upsert: this one and risansi-revenue.ts:43. Their month parsers diverge — this one lowercases to a 3-char key (accepts 'May-2026' and | Which sheet formats are accepted depends on which admin page the user happens to be on, and only one of the two writes revenue_upload_log —  | Delete this file and route its caller at /risansi/admin/revenue to the risansi-revenue.ts action, after moving the more forgiving parseMonth (this file's) into the survivor. | S |
| 14 | MEDIUM | CONFIRMED (verifie | SECURITY | app/actions/risansi-tasks.ts:141 | Same missing ownership check as deleteTask, plus 'status' is a compile-time-only union: server actions receive raw JSON, and tasks.status has no CHECK constraint (verified in pg_constraint), so any string persi | Any authenticated rep can close or reopen another rep's actions, and can write a junk status that no filter or KPI will ever match, silently | Add the same ownership gate as deleteTask, and guard the value: if (status !== 'open' && status !== 'completed') throw new Error('Invalid status'). Optionally add a CHECK constrain | S |
| 15 | MEDIUM | CONFIRMED | SECURITY | app/actions/risansi-visits.ts:506 | clientId is a separate client-supplied argument (VisitReportForm.tsx:924 passes visit.client_id from the browser) and is never checked against the visit's own client_id, even though the visit itself is authoris | A rep authorised on one visit can attach installed-base equipment — and the recordAudit entry at line 519 — to any client id they choose, in | Drop the clientId parameter and read it from the visit row already fetched at line 481: 'SELECT submitted_at, rep_id, client_id FROM visits WHERE id = $1', then insert that value.  | S |
| 16 | MEDIUM | CONFIRMED | CORRECTNESS | app/actions/risansi-visits.ts:310 | Three independent 'if's, and 'checked_by' is a member of both SAFE_SUGAR_COLS (line 274) and SAFE_NONSUGAR_COLS (line 279). Saving that one field on a sugar visit therefore upserts a row into visit_nonsugar_rep | It creates a phantom non-sugar report for a sugar client. visit_nonsugar_report holds 5 all-null rows and 1 visit (id 1446) with rows in bot | Make the dispatch exclusive (else-if) and route by the visit's actual industry_format, or rename the column in one of the two tables so a single key can never mean both. | S |
| 17 | MEDIUM | CONFIRMED | security | app/actions/risansi.ts:1772 | checkInVisit inserts the client-supplied repId straight into visits.rep_id with no validation against the session — unlike every other visit/opportunity path, which routes through resolveAssignableRepId. | A rep can check in attributed to any other user (or a nonexistent id, throwing an FK error the blanket catch converts to null). Visit covera | Ignore the payload repId for role 'rep' and resolve it server-side: 'const resolvedRepId = await resolveAssignableRepId(user, repId ?? null);' before the INSERT. | S |
| 18 | MEDIUM | CONFIRMED | security | app/actions/risansi.ts:873 | updateClientTier only calls requireSession — no canViewClient and no role gate — so any authenticated rep can change the tier of ANY client by id, and can set tier to any arbitrary string (no allow-list). | Tier drives segmentation/prioritisation views; a rep outside the client's tour shouldn't be able to modify it at all, and free-text values c | Add 'const viewer = await getCurrentUser(); if (!(await canViewClient(viewer, Number(clientId)))) throw ...' plus validate newTier against the known tier list. | S |
| 19 | MEDIUM | CONFIRMED | security | app/actions/risansi.ts:892 | assignClientTour lets anyone who can merely SEE a client move it to ANY tour — tourId is only checked to exist, not that the caller belongs to it, and there is no role gate (updateClient's tour_id path is admin | Tour is the entire visibility and ownership model (clientScopeSql). A rep can silently move a client onto a tour they're on (self-granting f | Restrict to hasRole(user.role,'manager')+ and require the target tour to be in getRepTours(user.id) for non-admins; or only allow setting tour_id when it is currently NULL (the sta | S |
| 20 | MEDIUM | CONFIRMED | security | app/actions/risansi.ts:793 | createOpportunity (line 793) and submitOpportunity (line 1805) accept any stage string except 'Won' — a crafted request can create an opp directly in 'Quoted', 'Negotiating' or 'Lost' (no lost_reason, no quotat | The Quoted-gateway rule and the cumulative required-field gate (lines 962-965) are enforced only on the pipeline path, so the server-side gu | Validate stage against CREATE_STAGES in both actions exactly as createPipelineOpportunity does at line 915, falling back to 'Suspect'. | S |
| 21 | MEDIUM | CONFIRMED | correctness | app/actions/risansi.ts:761 | planVisit (761) and assignVisit (1697) default the visit date to the UTC calendar day. On Vercel (UTC) any plan created before 05:30 IST is dated the previous day. Note also 'visitDate ?? ...': an empty-string  | India-based sales team: early-morning visit plans land on yesterday's calendar cell and in yesterday's coverage KPIs. | Compute the IST day explicitly, e.g. 'new Date(Date.now() + 5.5*3600e3).toISOString().slice(0,10)' or use Intl with timeZone 'Asia/Kolkata'; also treat '' as missing ('visitDate // | S |
| 22 | MEDIUM | CONFIRMED | correctness | app/actions/risansi.ts:1775 | checkInVisit swallows any INSERT error with an empty catch and returns null — the same value returned for a missing clientId or an authorization failure (line 1763). | A real failure (FK violation from a spoofed repId, DB outage, bad date) is indistinguishable from 'no access'; the rep in the field sees a g | Log the error (console.error with context) and either rethrow or return a discriminated result '{ ok: false, reason: 'no-access' / 'error' }' so the mobile UI can tell the user wha | S |
| 23 | MEDIUM | CONFIRMED | correctness | app/actions/risansi.ts:1842 | submitOpportunity's fallback insert silently drops rep_id, product_type, quote_ref, notes and created_by, and stuffs the free-text etaText into expected_close_date (a date-typed column in the primary schema). | If the full insert ever fails for a transient reason, the retry creates an ownerless, unattributed opportunity (rep_id NULL breaks the userC | Log the first error, and remove the fallback (the schema is stable now — every other path assumes the full column set) or at minimum carry rep_id and created_by into it and drop ex | S |
| 24 | MEDIUM | CONFIRMED | DATA LAYER | app/actions/sysadmin.ts:177 | setUserTours deletes every assignment then re-inserts one row at a time on the pool, with no BEGIN/COMMIT. deleteTour in risansi-reps.ts:159 does use a transaction, so the pattern exists in the codebase. | A failure or timeout after the DELETE leaves the user with zero tour assignments. Because visibility is tour-based (clientScopeSql), that us | Wrap in risansiPool.connect() + BEGIN/COMMIT/ROLLBACK exactly as deleteTour does, so the delete and the re-inserts stand or fall together. | S |
| 25 | MEDIUM | CONFIRMED (verifie | security | app/api/auth/[...nextauth]/route.ts:15 | A hardcoded JWT signing secret is committed to the repo as the fallback. If NEXTAUTH_SECRET is ever unset/misnamed in a Vercel environment, session JWTs are signed with a publicly-known value. | Anyone could mint a token carrying an existing sysadmin's email; the jwt callback then loads that user's real role from the DB, giving full  | Drop the fallback and fail fast: 'const secret = process.env.NEXTAUTH_SECRET; if (!secret) throw new Error('NEXTAUTH_SECRET is not set');'. Rotate the secret if the fallback was ev | S |
| 26 | MEDIUM | CONFIRMED | correctness | app/api/auth/[...nextauth]/route.ts:102 | Any transient failure of the per-request users lookup silently downgrades the session to Pending/rep. proxy.ts:19 then redirects on 'token.risansiAccess !== 'Approved'' to /api/auth/signup/pending. | A momentary Postgres blip or pool exhaustion bounces every signed-in user to the 'Request submitted' screen and strips their role, looking l | On catch, keep the previous token values ('token.risansiAccess ??= ...') rather than overwriting, or re-throw so next-auth surfaces a 500; only write 'Pending' when the query succe | S |
| 27 | MEDIUM | CONFIRMED | data-layer | app/api/auth/[...nextauth]/route.ts:42 | Out-of-batch but load-bearing for lib/risansi-auth.ts: users has no UNIQUE constraint or index on email (pg_constraint shows only pkey, NOT NULLs, role/status CHECKs). authorize() and the jwt callback (line 83) | Two rows for one address (0 today, nothing prevents it) let the row whose password was verified differ from the row whose role and repId ent | 'CREATE UNIQUE INDEX CONCURRENTLY users_email_lower_key ON users (lower(email));' then add the matching constraint, and make the jwt callback select by 'token.repId' rather than re | S |
| 28 | MEDIUM | CONFIRMED | ui-ux | app/api/auth/signin/page.tsx:48 | Every field on the sign-in and request-access forms uses a bare '<label>' with no htmlFor and an '<input>'/'<select>' with no id or aria-label (signin/page.tsx:48,57; signup/page.tsx:68,76,87,95,106). AUTH_LABE | Screen readers announce these as unlabelled edit fields, and clicking the label does not focus the control — on the two pages every user mus | Give each input an id and each label a matching htmlFor ('<label htmlFor="signin-email">' / '<input id="signin-email">'), or wrap the input inside the label element. | S |
| 29 | MEDIUM | CONFIRMED | security | app/api/auth/signup/submit/route.ts:46 | The raw exception message is returned to an unauthenticated caller. A pg error surfaces table, column, and constraint names. app/api/debug-db/route.ts:25 does the same ('{ error: err.message }'). | Free schema disclosure to anonymous users on an internet-facing endpoint. | Log the error server-side and return a fixed '{ error: 'Failed to submit request' }' with 500. Same in debug-db (or delete that route per the finding above). | S |
| 30 | MEDIUM | CONFIRMED | correctness | app/api/auth/signup/submit/route.ts:11 | No type validation on the JSON body. A non-string 'email', 'name', or 'password' throws a TypeError ('.endsWith'/'.trim' is not a function) and falls into the catch, returning a 500 with the raw message. The su | Malformed public input produces 500s instead of 400s, and the case-sensitive domain check rejects legitimate addresses when the API is calle | Validate with a zod schema ('z.object({ name: z.string().trim().min(2), email: z.string().email(), password: z.string().min(8), role: z.enum(['rep']) })'), lowercase before the dom | S |
| 31 | MEDIUM | CONFIRMED | CORRECTNESS | app/api/risansi/clients-search/route.ts:65 | Every failure - DB down, SSL timeout, syntax error - is swallowed and returned as an empty 200 array, with no console.error either (unlike the sibling routes). | The search box shows 'no results' during an outage, so a rep concludes the client does not exist and creates a duplicate. The pool-exhaustio | '} catch (err) { console.error('[clients-search]', err); return Response.json({ error: 'Search failed' }, { status: 500 }); }' and render an error state distinct from empty. | S |
| 32 | MEDIUM | CONFIRMED | SECURITY | app/api/risansi/industries/route.ts:4 | No session check; same in app/api/risansi/competitors/route.ts:4 ('SELECT id, name FROM competitors WHERE is_active = TRUE'). Both are publicly fetchable because proxy.ts does not match /api/*. | Leaks the industry segmentation and the tracked-competitor list to anyone with the URL. Low value individually, but it confirms the /api/ris | Add the 'getCurrentUser()' + 'if (!user.email) return 401' preamble the sibling routes use; better, add '/api/risansi/:path*' to the proxy matcher so unauthenticated calls never re | S |
| 33 | MEDIUM | CONFIRMED | SECURITY | app/api/risansi/opportunities/[id]/items/route.ts:13 | Four routes never assert a session and rely on the visibility predicate to deny: this one, clients-search:11, client-pumps:11, quotation GET:112. getCurrentUser() returns id null with no session, and intOrNull( | Fail-open shape. The deny depends on no row existing with rep_id = 0 in tour_assignments or client_rep_access; I confirmed both are 0 today  | Add an explicit 'if (!user.email) return 401' to all four, and make intOrNull reject explicitly: 'if (v === null // v === undefined // v === '') return null;' before the Number() c | S |
| 34 | MEDIUM | CONFIRMED | CORRECTNESS | app/api/risansi/opportunities/[id]/quotation/route.ts:78 | POST upserts opportunity_quotation_files then separately UPDATEs opportunities.quotation_link; DELETE (lines 101-102) drops the file row then clears the link. Two statements each, no transaction. | A failure between them leaves the pair inconsistent: on POST a stored PDF nobody can reach, on DELETE a quotation_link pointing at a route t | Wrap each pair in a pool.connect() with BEGIN / COMMIT / ROLLBACK in try-catch-finally, releasing the client in finally. | S |
| 35 | MEDIUM | CONFIRMED | SIMPLIFICATIO | app/api/risansi/opportunities/[id]/quotation/route.ts:19 | The opportunity-edit rule exists three times: canEditOpp here, an inline copy in opportunities/[id]/stage/route.ts:55-64, and userCanEditOpp in app/actions/risansi.ts:161. In two of them the manager branch is u | Three copies of one authorisation rule drift. The stage route's own comment records a past incident where UI and server disagreed and 984 op | Export userCanEditOpp from lib/risansi-auth.ts, import it in both routes, delete the copies, and drop the unreachable manager branch or move it above the clientId check if it was m | S |
| 36 | MEDIUM | CONFIRMED (verifie | SECURITY | app/api/risansi/opportunities/[id]/stage/route.ts:17 | No route under app/api/risansi/** checks session.user.risansiAccess (zero grep hits outside the nextauth callbacks). proxy.ts blocks non-Approved users on page routes only, and revokeAccess (app/actions/risansi | Revoking access does not stop API reads or writes. A revoked rep can still move stages, upload photos, delete quotation PDFs and pull the cl | Add requireApprovedSession() to lib/risansi-auth.ts rejecting unless risansiAccess === 'Approved', use it in all 25 routes in place of the ad-hoc checks, and set is_active = FALSE  | S |
| 37 | MEDIUM | CONFIRMED | SECURITY | app/api/risansi/visit-photo/[photoId]/route.ts:26 | PATCH (caption) and DELETE (line 97, 'DELETE FROM visit_photos WHERE id = $1') are gated only on canViewClient - can see the client - not on who owns the visit. Any of the 304 tour assignees can rename or perma | Destructive and irreversible (bytea dropped, no soft delete, no undo). lib/risansi-auth.ts:214 already provides canEditVisitReport(user, vis | Have checkPhotoAccess also select v.rep_id and gate PATCH/DELETE on 'canEditVisitReport({role:user.role, repId:user.id}, visitRepId)', leaving GET on canViewClient. | S |
| 38 | MEDIUM | CONFIRMED | SIMPLIFICATIO | app/risansi/admin/revenue/RevenueUploadClient.tsx:31 | The entire file is dead code: a repo-wide grep finds it referenced only by itself. The live revenue page (revenue/page.tsx) mounts RevenueUploadBox, never this component. It is a second, drifting copy of the up | A dead duplicate upload path rots, confuses maintainers, and invites edits that never ship. | Delete app/risansi/admin/revenue/RevenueUploadClient.tsx after confirming no dynamic import references it. | S |
| 39 | MEDIUM | CONFIRMED | GOVERNANCE | app/risansi/clients/[id]/page.tsx:31 | Every one of the 13 Client-360 data queries is wrapped in q(), which catches ALL errors and returns the fallback (empty array / zeroed struct) with no logging. | A failed revenue/opportunities/pumps query renders indistinguishably from genuinely-empty data (₹0, 'No opportunities'), silently misleading | Log inside the catch: 'catch (err) { console.error('[client/360] query failed', err); return fallback; }', and consider a per-panel error state so a failed financial query shows 'd | S |
| 40 | MEDIUM | NEEDS-CHECK | SECURITY | app/risansi/clients/[id]/page.tsx:680 | DB-sourced free-text URLs (client.google_maps_url at lines 680 & 1175, opportunity quotation_link at 1448) are rendered directly into <a href> with no scheme validation. | React 19 does not strip 'javascript:' URLs at runtime (dev-only warning), so an edit-capable user who stores 'javascript:...' in google_maps | Validate scheme before rendering: only emit the anchor when the value matches /^https?:\/\//i (or new URL(v).protocol is http/https); otherwise render as plain text. Enforce at wri | S |
| 41 | MEDIUM | CONFIRMED | CORRECTNESS | app/risansi/compete/page.tsx:388 | displacementAccounts is capped at LIMIT 50 (p.218), yet its length drives the 'Displacement Targets' KPI (p.449) and totalCompetitorUnits feeds the KPI sub-label (p.451) and the warn Tag (p.524), both presented | When more than 50 accounts have competitor presence, the headline 'Displacement Targets' maxes out at 50 and 'competitor pumps in play' unde | Compute the count and total competitor units in SQL (a separate uncapped COUNT/SUM aggregate over the same dispWhere), and keep LIMIT 50 only for the displayed table rows. | S |
| 42 | MEDIUM | CONFIRMED | UI/UX | app/risansi/loading.tsx:15 | Skeleton boxes use className="shimmer", but no '.shimmer' CSS rule exists — globals.css defines '.skeleton' (with the shimmer keyframes) instead. The boxes set only size/radius, no background, so they render in | All loading states (this file plus visits/, clients/, clients/[id] loaders) show blank empty space during data fetch instead of the intended | Rename '.skeleton' to '.shimmer' in globals.css (or add a '.shimmer' alias), or change the loaders to className="skeleton". | S |
| 43 | MEDIUM | CONFIRMED | SECURITY | app/risansi/mobile/visit/new/NewVisitClient.tsx:55 | The mobile check-in sends a client-supplied repId to the checkInVisit server action, which inserts it as visits.rep_id after validating only that the CLIENT is viewable (canViewClient) - it never checks repId e | Any authenticated rep can create a checked-in visit attributed to a different rep for any client on their tour, falsifying Executive-Review  | In checkInVisit, ignore the passed repId and resolve it from the session (session.user.repId / users lookup by email), or reject when it differs; drop repId from the client payload | S |
| 44 | MEDIUM | CONFIRMED | GOVERNANCE | app/risansi/pipeline/page.tsx:22 | The q() wrapper (pipeline p.22-24, identical in compete p.9-11) and the stage page's '.catch(() => [])' (stage p.106) swallow every query error with no logging, returning an empty array or 0. | A DB blip, schema drift, or a bug in any inlined-SQL fragment silently renders fabricated zeros (Booked ₹0, empty Won total) or an empty tab | Log the caught error (console.error + Sentry/monitoring) before returning the fallback, and/or render a distinct 'data unavailable' state instead of an empty/zero one so an error i | S |
| 45 | MEDIUM | CONFIRMED | UI/UX | app/risansi/revenue/page.tsx:58 | The rep-only 'My Revenue / All Revenue' toggle changes only URL/subtitle state; every one of the 9 data queries uses repCond = clientVisibilitySql(currentUser) unconditionally, which for a rep is always their t | Both toggle states render identical numbers, and 'All Revenue' is a misnomer (it is not company-wide) - a control that visibly does nothing  | Either remove the toggle for reps, or make 'personal' actually narrow the predicate (e.g. own-owned clients) vs 'full' (full tour scope) and thread it into repCond. | S |
| 46 | MEDIUM | CONFIRMED | GOVERNANCE | app/risansi/revenue/page.tsx:13 | The q() wrapper swallows every query error and returns the fallback with no logging. Same silent-catch in app/risansi/mobile/page.tsx:13, visits/coverage/page.tsx:12, and visits/[id]/page.tsx:13 (exec-review an | A broken query (schema drift, timeout) renders zeros/empty tiles indistinguishable from genuine no-data, with no trace anywhere - silent fai | Log inside the catch (console.error with a page tag) like the exec-review/complaints wrappers do, and feed a real error-monitoring sink. | S |
| 47 | MEDIUM | CONFIRMED | CORRECTNESS | components/risansi/EditOppDrawer.tsx:565 | DeleteOppButton awaits deleteOpportunity with no try/catch. On any failure (permission denial, FK/cascade error — deleteOpportunity is known to cascade) the promise rejects unhandled, onDeleted() never runs, an | User gets no error, the modal hangs in a half-deleted state, and an unhandled rejection surfaces as a generic Next error — unlike handleSubm | Wrap in try/catch: on error setLoading(false) and render an error message (reuse the drawer's error state); only call onDeleted() on success. | S |
| 48 | MEDIUM | CONFIRMED | CORRECTNESS | components/risansi/VisitReportForm.tsx:1746 | The Expansion/New Business section's debounced auto-save discards every error ('.catch(() => {})'). A failed save is invisible; unlike every other save path in this same form (saveClientProfile shows ✓/⚠, useAu | If the last debounced save before the rep navigates away/submits fails, the expansion opportunity (value, tagged TSM) is silently lost — sub | Surface failure like the sibling paths: set an error/'unsaved' state in the .catch and render a ⚠ indicator near the section (and ideally re-queue). At minimum flip a 'saveFailed'  | S |
| 49 | MEDIUM | CONFIRMED (verifie | correctness | lib/db-risansi.ts:21 | The pg Pool is created and exported with no 'pool.on('error', ...)' listener anywhere in the repo (grepped: zero matches outside .claude/). | node-postgres emits 'error' on the Pool when an idle client's socket dies; with no listener EventEmitter rethrows and the process crashes. 1 | Immediately after the Pool is constructed: 'risansiPool.on('error', (err) => console.error('[pg pool] idle client error', err));' (and forward to a monitor). Do not process.exit on | S |
| 50 | MEDIUM | CONFIRMED | correctness | lib/risansi-auth.ts:61 | intOrNull(null) returns 0, not null, because Number(null) === 0 and Number.isInteger(0) is true. Every 'if (uid == null) return 'FALSE'' guard below it (lines 79, 97, 109) is therefore unreachable for a user wi | The documented fail-closed branch never runs; visibility becomes 'rep_id = 0', safe only by accident. A single seeded row with id 0 in users | 'function intOrNull(v: unknown) { return typeof v === 'number' && Number.isInteger(v) && v > 0 ? v : null; }' | S |
| 51 | MEDIUM | CONFIRMED | performance | lib/risansi-auth.ts:50 | Neither getCurrentUser() nor requireSession() memoizes the session per request. next-auth 4.24.14 runs 'callbacks.jwt' on every getServerSession (node_modules/next-auth/core/routes/session.js:53), and this app' | ~119 getServerSession/getCurrentUser call sites. app/risansi/layout.tsx:13 wraps its own in React cache() but lib does not, so a layout plus | 'import { cache } from 'react'' in lib/risansi-auth.ts and export 'getCurrentUser = cache(async () => {...})' plus a cached 'getSession()'; have requireSession reuse it. | S |
| 52 | MEDIUM | CONFIRMED | correctness | lib/risansi-money.ts:36 | parseMoneyInput silently truncates at the first non-numeric character instead of rejecting: '1.5 Cr' parses to 1.5, '15 lakh' to 15, '2L' to 2. | Exactly the failure class this file's header documents (offer_value_inr rows saved as 1). Callers write the result straight to offer_value_i | After stripping separators, require the whole string to be numeric: 'if (!/^-?\d+(\.\d+)?$/.test(s)) return null;' — MoneyInput already renders "Couldn't read an amount from that"  | S |
| 53 | MEDIUM | CONFIRMED | workflow | lib/risansi-notify.ts:43 | claimRun (the only replay guard for the cron sweeps) fails open: any error from the notification_runs INSERT — connection drop, timeout, lock — is treated as 'window not yet claimed', so the sweep proceeds. | A Vercel cron retry or a double-fire during a DB hiccup re-blasts every sysadmin with the escalation digest and every manager with the weekl | Distinguish 'relation does not exist' (pg code 42P01 → return true) from every other error (return false and log). The table exists in prod (migration 0036), so the fallback only m | S |
| 54 | MEDIUM | CONFIRMED | DATA LAYER | migrations (schema_migrations) | 7 migrations (0041-0047) applied out-of-band this session, not via scripts/migrate.mjs; schema_migrations has 40 rows vs 47 files, so a migrate run re-executes them. | Migration tracking is out of sync; a routine migrate re-runs 7 files. Safe only because each was written idempotently. | INSERT the 7 version rows into schema_migrations ON CONFLICT DO NOTHING; apply via migrate.mjs only going forward. | S |
| 55 | MEDIUM | CONFIRMED | SIMPLIFICATIO | app/actions/risansi-admin.ts:29 | approveAccessRequest / rejectAccessRequest / revokeAccess duplicate approveUser / rejectUser / revokeUser in admin.ts:32,113,124. One set keys on email, the other on users.id, and only admin.ts also sets the ro | Approving through this file leaves role and tour assignments untouched, so the user is Approved but sees nothing (tour-based visibility). Tw | Keep the admin.ts (id-keyed, role + tours) versions, resolve email→id in the callers of this file, and delete risansi-admin.ts. | M |
| 56 | MEDIUM | CONFIRMED | DATA LAYER | app/actions/risansi-outstanding.ts:51 | uploadOutstanding nulls the outstanding figures on every client before applying the new sheet. It is transactional, so atomic, but the previous per-client values are overwritten in place with no history table. | Uploading the wrong month's file, or a truncated export, destroys the prior outstanding position for every client and there is no restore pa | Insert the prior (client_id, total_outstanding, outstanding_as_of) rows into a client_outstanding_history table inside the same transaction before the wipe, keyed by the new log id | M |
| 57 | MEDIUM | CONFIRMED | PERFORMANCE | app/actions/risansi-pumps.ts:76 | uploadPumps issues one round trip per pump row, outside any transaction. risansi-revenue.ts:83 is worse — an existence SELECT plus an upsert per row, two round trips each. | An ERP installed-base export is thousands of rows; at Vercel-to-Postgres latency that is minutes of serial round trips and a likely function | Batch the upsert with a multi-row VALUES insert (or UNNEST($1::text[], $2::text[], ...)) in chunks of ~500 inside a transaction, setting the log status in the same transaction. For | M |
| 58 | MEDIUM | CONFIRMED | SECURITY | app/actions/risansi-tasks.ts:98 | addTask (and createComplaint at risansi-complaints.ts:128) will send mail from the company Resend domain to any address the caller types, with a caller-chosen title and description. A repo-wide grep for ratelim | Any authenticated rep has an unmetered outbound mailer on the company's sending domain with free-text body content — a spam/phishing relay a | Rate-limit external-recipient sends per user (a sends-per-hour counter keyed on users.id, checked before notifyActionRecipient), and consider restricting external addresses to an a | M |
| 59 | MEDIUM | CONFIRMED | CORRECTNESS | app/actions/risansi-visits.ts:776 | submitVisit closes the visit, then inserts displacement opportunities (line 792), a follow-up task (line 812), a planned follow-up visit (line 831) and updates clients.last_visit_date (line 843) — five separate | A failure after step 1 leaves the report irreversibly closed while the opportunities and follow-up task it was supposed to generate never ex | Take a single client from risansiPool.connect() and run steps 1-5 inside BEGIN/COMMIT, keeping only the best-effort notification calls outside the transaction. | M |
| 60 | MEDIUM | CONFIRMED | correctness | app/actions/risansi.ts:1215 | saveQuotedDetails runs the stage UPDATE, syncOfferRevisions, and a DELETE-then-loop-INSERT replacement of opportunity_items as separate pool queries — the item replacement is not in any transaction. | A failure after the DELETE (bad value, connection drop, Vercel timeout) leaves the quote with zero line items while the header says Quoted;  | Wrap the whole function body (UPDATE + revisions + item replacement + stage log) in one client transaction, mirroring the BEGIN/COMMIT pattern already used in syncOfferRevisions an | M |
| 61 | MEDIUM | CONFIRMED | correctness | app/actions/risansi.ts:1085 | createPipelineOpportunity commits the opportunity row first, then inserts offer revisions (1081) and quoted items (1088-1096) as separate post-commit queries; only the Won+SO case is transactional. | A mid-loop failure creates an opportunity whose offer total exists but whose line items / revision history are partially or wholly missing — | Use one connection/transaction for insert + revisions + items + stage log (extend the existing soRows transaction branch to cover every create). | M |
| 62 | MEDIUM | CONFIRMED | DATA LAYER | app/actions/sysadmin.ts:152 | With force=true the ownership guard at line 125 is skipped. visits_rep_id_fkey is ON DELETE SET NULL and opportunities_rep_id_fkey has no action (verified in pg_constraint). | A forced delete silently nulls rep_id on every visit that user ever filed — those visits vanish from rep-scoped views and Executive Review — | Before deleting, require reassignment: take a new_owner_id and UPDATE visits/opportunities/tasks SET rep_id = $new WHERE rep_id = $old inside a transaction, then delete. Or deactiv | M |
| 63 | MEDIUM | CONFIRMED | security | app/api/auth/[...nextauth]/route.ts:30 | No rate limit, throttle, or lockout on credential login. auth_audit already holds 91 login_failed rows and nothing consumes them; the only reader is a read-only admin dashboard (app/risansi/admin/audit/page.tsx | Unlimited online password guessing against a known, small set of @risansi.com accounts; also unbounded bcrypt work per request on a serverle | Before bcrypt.compare, count auth_audit login_failed rows for that email/IP in the last 15 min and return null past ~8; or add an Upstash/Vercel KV fixed-window limiter keyed on lo | M |
| 64 | MEDIUM | CONFIRMED | governance | app/api/auth/[...nextauth]/route.ts:103 | No tests, no CI workflow (.github/workflows absent), and no error monitoring (no Sentry/OTel in package.json) anywhere on the auth or cron paths. Failures are console.error only, and next.config.ts strips all c | A broken cron auth check, a failing digest, or a JWT-callback regression is invisible until a human notices missing emails or a locked-out t | Add @sentry/nextjs (captureException in the jwt catch and each 'catch (e)' in risansi-notify), have the cron routes return non-200 when a sweep throws so Vercel Cron marks the run  | M |
| 65 | MEDIUM | CONFIRMED | security | app/api/auth/change-password/route.ts:51 | Changing a password does not invalidate existing sessions. Sessions are stateless JWTs with an 8h maxAge and the jwt callback only re-checks status/role/is_active — never a password version. | A user who changes their password because they suspect compromise leaves the attacker's stolen JWT valid for up to 8 more hours; the same ga | Add a 'users.token_version int NOT NULL DEFAULT 0', bump it in this UPDATE and in resetUserPassword, stamp it into the JWT on sign-in, and in the jwt callback clear the token when  | M |
| 66 | MEDIUM | CONFIRMED | security | app/api/auth/signup/submit/route.ts:36 | The signup POST is unauthenticated, unrate-limited, and returns a distinct 409 for an already-registered address, turning it into a staff-account enumeration oracle. Each call also runs bcrypt.hash(cost 10) and | Anyone can enumerate every @risansi.com employee account, and can flood the users table / burn serverless CPU with unlimited bcrypt hashing. | Return the same 200 'request submitted' body for both new and duplicate emails (log the collision server-side), and add an IP-keyed rate limit (e.g. 5/hour) before the bcrypt.hash  | M |
| 67 | MEDIUM | CONFIRMED | performance | app/api/cron/daily/route.ts:13 | Both reminder sweeps loop 'for (const t of rows)' with a sequential 'await sendNotification(...)' up to 'LIMIT 300' each (lib/risansi-notify.ts:70,84,108,121). At typical Resend latency 600 serial sends cannot  | The function is killed mid-loop; unstamped rows silently get no reminder that day and the route never reports the truncation (it always retu | Batch the sends with bounded concurrency (p-limit 8, or chunks of 10 via Promise.all) and stamp in one 'UPDATE tasks SET last_reminded_at = NOW() WHERE id = ANY($1)' per chunk; ret | M |
| 68 | MEDIUM | CONFIRMED | SECURITY | app/api/risansi/bugs/route.ts:73 | No rate limiting exists anywhere in app/, lib/ or components/ (grep for rate-limit/throttle/upstash hits only skill docs). This route takes an 8 MB bytea and fires a Resend email per call; visits/[visitId]/phot | One authenticated user or one runaway retry loop can burn the Resend quota and inflate the bytea tables without bound - visit_photos is alre | Per-user token bucket keyed on user.id (rate_limits table or Vercel KV): ~10 bugs/hour, ~50 uploads/hour, return 429. Also cap photos per visit with a COUNT check before INSERT. | M |
| 69 | MEDIUM | CONFIRMED | GOVERNANCE | app/api/risansi/opportunities/[id]/stage/route.ts:1 | No tests directory and no test script in package.json (only dev/build/start/lint/migrate), so none of the 25 routes has a regression test - including the three copies of the opportunity-edit rule. Logging is co | These authorisation rules have already regressed once (this route's comment describes 984 opportunities silently 403ing), and on Vercel a co | Add vitest with route tests calling each handler under mocked getServerSession for rep/manager/admin/no-session, asserting status - start with stage, quotation, visit-photo, valida | M |
| 70 | MEDIUM | CONFIRMED | COST | app/api/risansi/opportunities/export/route.ts:299 | Neither export has a LIMIT, and this one allocates a dataValidation object per cell across ~30 columns x (rows + 200), building the whole workbook in memory before the first byte. At 1,731 opportunities that is | Cost and reliability scale linearly with the tables against a fixed serverless memory ceiling and wall clock, so it fails abruptly rather th | Scope validation to rows.length + 20 instead of a fixed +200, cap the export at a configurable max (e.g. 20,000 rows) with a clear message above it, and stream via ExcelJS Workbook | M |
| 71 | MEDIUM | CONFIRMED | SECURITY | app/risansi/field/page.tsx:477 | The Activities/Action-Register task query only scopes when isRep (role==='rep'). Managers (isRep=false, non-admin) fall to the empty scope and receive ALL tasks company-wide (LIMIT 200) plus the openTaskCount,  | Every other query on this page scopes managers by tour via clientScopeSql/clientVisibilitySql (manager = non-admin). This one leaks the whol | Scope non-admins, not just reps: build the WHERE from repVisibilitySql()-style logic (as lib/risansi-action-queue does) whenever !hasRole(role,'admin'), passing repId. Reuse buildT | M |
| 72 | MEDIUM | CONFIRMED | PERFORMANCE | app/risansi/field/page.tsx:381 | The Map-tab client query has no LIMIT. For admin/sysadmin (no cVisAnd restriction) it returns every active, non-deleted client and ships the full set to the browser via IndiaMapWrapper + ClientCoverageList on e | Unbounded result set grows with the client base (hundreds+ already); large payload and render cost, and it always runs even when the Map tab | Add a sane LIMIT and/or only run the query when tab==='map' (mirror the tab!=='reports' guard on query 7). Consider server-side clustering/aggregation for the map. | M |
| 73 | MEDIUM | CONFIRMED | CORRECTNESS | app/risansi/page.tsx:22 | The q() wrapper swallows every error and returns the fallback (0 / [] / {}) with no logging. Any DB error or query bug silently renders the executive dashboard with ₹0 revenue, empty pipeline, zero market share | Silent, misleading data on the top executive view with no signal to anyone — a broken query looks identical to 'no data'. No error monitorin | Log the caught error (console.error with query label at minimum, ideally a real error reporter) before returning the fallback, and consider surfacing a per-panel error state instea | M |
| 74 | MEDIUM | LIKELY | PERFORMANCE | app/risansi/pipeline/page.tsx:339 | The open-opps query is deliberately uncapped, selects o.* (every column incl. notes, negotiation_notes, lost_reason, drop_reason) plus several per-row correlated subqueries, and the full result is handed to two | All open rows with all columns are serialized into the RSC payload to the browser (twice, table + kanban) and the correlated subqueries run  | Select only the columns the table/kanban use, and paginate or cap the client-shipped set (the table already slices to 50 client-side); consider a lighter kanban feed than full rows | M |
| 75 | MEDIUM | CONFIRMED | ui-ux | components/risansi/NewOpportunityModal.tsx:95 | Modal dialogs have no role='dialog'/aria-modal, no focus trap, and no Escape-to-close. Affects NewOpportunityModal, OppCompletionModal (L58), QuotedDetailsModal (L186) and ReportBugButton (L88). Only backdrop-c | Keyboard and screen-reader users can't escape or be scoped to the dialog; focus leaks to the page behind and there is no non-pointer dismiss | Add role='dialog' aria-modal='true' aria-label, an Escape keydown handler, and initial-focus + focus-trap (or a shared Modal wrapper) across the four modals. | M |
| 76 | MEDIUM | CONFIRMED | ui-ux | components/risansi/OppCompletionModal.tsx:210 | Hardcoded light colors: modal body is background:'white' (L60) and INPUT is background:'white' with color:'var(--fg)'. In dark mode var(--fg) is light, so text is light-on-white = unreadable. Same in RepRow (IN | Dark mode is shipped (tokens flip via :root.dark); these surfaces render invisible or wrong-contrast text for dark-theme users. | Replace literal white/#EBF1FB/#FDE8E8/#D1FAE5/#F1F5F9/#FFF8F8 with theme tokens (var(--bg-paper), var(--bg-elev), var(--neg-soft), var(--pos-soft), etc.). | M |
| 77 | MEDIUM | CONFIRMED | security | lib/risansi-auth.ts:54 | getCurrentUser() fabricates an identity for an anonymous caller ('id: null, email: null, role: 'rep'') instead of returning null or throwing. | app/api/risansi/clients-search/route.ts has no session check at all and leans on it entirely. It fails closed only because the null id degra | Add 'export async function getRequiredUser(): Promise<CurrentUser>' that throws when 'session?.user' is absent, and call it from every /api/risansi route; make getCurrentUser retur | M |
| 78 | MEDIUM | CONFIRMED | security | lib/risansi-auth.ts:27 | No shared helper checks 'risansiAccess === 'Approved'' or re-checks is_active at request time; requireSession() accepts any decodable session, and getCurrentUser() likewise. | proxy.ts enforces risansiAccess only for /risansi/* and /admin/*; /api/risansi/* is outside the matcher. A revoked, Pending or deactivated u | Have getCurrentUser() carry 'access' from 'session.user.risansiAccess' and add 'requireApprovedUser()' that rejects anything but 'Approved'; use it in every /api/risansi route hand | M |
| 79 | MEDIUM | CONFIRMED | governance | lib/risansi-auth.ts:94 | The authorisation core has zero automated tests and the repo has no CI: no tests/ or __tests__/ directory, no .github/workflows, and package.json scripts are only dev/build/start/lint/migrate. | clientScopeSql / clientVisibilitySql / canViewClient / hasRole gate every read path; a regression returning null (unrestricted) instead of a | Add vitest plus a table-driven suite over hasRole, clientVisibilitySql, clientScopeSql and intOrNull for rep/manager/admin/sysadmin/unknown-role/null-id, asserting the exact SQL an | M |
| 80 | MEDIUM | CONFIRMED | governance | lib/risansi-email.ts:37 | Every send failure is swallowed: sendEmail returns {ok:false} and no caller inspects the result (e.g. app/actions/risansi-tasks.ts:47 'await notifyActionAssigned({...})' discards it); an unset key only produces | With no error monitoring and console-only logging, a revoked or rate-limited Resend key stops assignment, complaint, visit-plan and escalati | Persist failures — 'recordAudit({ action: 'email_failed', entityType, summary: error })' from sendEmail — and add an admin health tile counting email_failed rows in the last 24h. | M |
| 81 | MEDIUM | CONFIRMED | workflow | lib/risansi-notify.ts:171 | Both deduped sweeps (reached from app/api/cron/*) claim their window BEFORE the send loop — weekly at :171, admin escalation at :149. If the loop throws after the first recipient, the catch swallows it and the  | Silent partial delivery of the weekly digest and the >5-day escalation, with the cron route still returning '{ ok: true }'. | Claim per recipient ('claimRun('manager_digest:'+m.id, ...)') inside the loop, or wrap each send in its own try/catch and delete the claim row if zero sends succeeded. | M |
| 82 | MEDIUM | CONFIRMED | SECURITY | lib/risansi-visit-filters.ts:19 | zone/tour/rep filter values come straight from the URL query string (sp.zone/tour/rep in parseVisitFilters) and are inlined into SQL IN(...) lists via manual single-quote escaping, not parameter binding. The he | Only safe because quote-doubling + standard_conforming_strings neutralise the sole break-out character. It is one lax edit (another quote ch | Bind as parameters: build 'c.tour_id IN (SELECT id FROM tour_routes WHERE zone = ANY($n::text[]))' and push the array into vals, mirroring buildOppFilter. Remove esc/arr string-bui | M |
| 83 | MEDIUM | CONFIRMED | SECURITY | next.config.ts:17 | Only response header set is Permissions-Policy: geolocation=(self). No HSTS, X-Frame-Options/frame-ancestors, X-Content-Type-Options, or CSP. | App is clickjackable (no frame protection) and has no CSP; MIME-sniffing not disabled globally (compounds the quotation stored-XSS). | Add headers(): X-Frame-Options DENY, X-Content-Type-Options nosniff, Strict-Transport-Security, a baseline CSP. | M |
| 84 | MEDIUM | CONFIRMED | governance | app/actions/risansi.ts:1 | 2048 lines of money-moving, permission-enforcing server actions with zero automated tests anywhere in the repo (no test files outside node_modules), no error monitoring, and console.error-only logging for swall | The file encodes every business rule (Quoted gateway, Won-requires-SO, crore conversion, owner derivation) purely in hand-verified code; reg | Add a minimal integration test suite (Vitest + a test schema or pg-mem) covering: stage-gate enforcement, Won+SO atomicity, updateOpportunity field preservation, and the authz gate | L |
| 85 | LOW | CONFIRMED | CORRECTNESS | app/actions/admin.ts:69 | approveUser swallows every failure of the per-tour INSERT, and auditTourAssignment at line 29 swallows its own. risansi-revenue.ts:132 does the same for the upload log. | A sysadmin approving a rep with three tours can silently get zero of them assigned; the page reports success and the rep signs in to an empt | Collect the failures and either throw with the list of tour ids that did not stick, or return them to the caller so the UI can show "2 of 3 tours assigned". | S |
| 86 | LOW | CONFIRMED | CORRECTNESS | app/actions/admin.ts:34 | approveUser, rejectUser (line 115), revokeUser (line 126) and reapproveUser (line 138) parseInt without a NaN guard, unlike resetUserPassword at line 89 which does check. | A missing or malformed id sends 'WHERE id = NaN' to Postgres, producing an unhandled 22P02 invalid-input-syntax error surfaced to the user a | Reuse the guard already written at line 89: if (!Number.isInteger(id) // id <= 0) throw new Error('Invalid user id'). | S |
| 87 | LOW | NEEDS-CHECK | SIMPLIFICATIO | app/actions/risansi-outstanding.ts:12 | A non-function value export from a 'use server' module, referenced only inside this file (line 67). risansi-complaints.ts:49-50 documents the opposite rule: "a 'use server' module may only export async function | Either the documented constraint is wrong or this is a latent build break waiting on the next Next.js upgrade. The map also hardcodes six us | Drop the 'export' keyword (nothing imports it), and move the debtor-code→user mapping into app_settings or a lookup table so it can be corrected without a deploy. | S |
| 88 | LOW | CONFIRMED | SECURITY | app/actions/risansi-reps.ts:94 | createRep (line 29) and updateRep accept an arbitrary role string. users_role_check restricts it to rep/manager/admin/sysadmin, so the DB rejects garbage — but as an unhandled 23514. admin.ts:11 has a VALID_ROL | Both actions are sysadmin-gated so this is not privilege escalation, but a mistyped role surfaces as a raw Postgres constraint error. update | Import a shared VALID_ROLES from one place and validate before the write; pre-check email uniqueness in updateRep the way createRep does. | S |
| 89 | LOW | CONFIRMED | PERFORMANCE | app/actions/risansi-tasks.ts:103 | pg_indexes shows tasks has exactly one index, tasks_pkey. client_id, assigned_to_rep, visit_id and status are all unindexed despite being the columns every task read filters on (and the three FK columns). | Every Action Registry, Client 360 activity list and dashboard action-queue read is a sequential scan of tasks, and each of the four revalida | CREATE INDEX on tasks(client_id), tasks(assigned_to_rep) WHERE status = 'open', and tasks(visit_id). | S |
| 90 | LOW | CONFIRMED | CORRECTNESS | app/actions/risansi-tasks.ts:111 | addTask writes assignedToRep straight through with no check that it is a real, active user. risansi-complaints.ts:67 has validateAssignee doing exactly that check for the equivalent field. | tasks_assigned_to_rep_fkey catches a nonexistent id, but an inactive user passes, producing an action assigned to someone who can never sign | Reuse the complaints check: SELECT 1 FROM users WHERE id = $1 AND is_active = TRUE before the insert, and throw if it does not match. | S |
| 91 | LOW | CONFIRMED | CORRECTNESS | app/actions/risansi-visits.ts:834 | next_visit_recommendation is a DATE; node-postgres parses it to a JS Date at local midnight, and toISOString() converts to UTC before slicing the day. | Correct only because the database and the Vercel runtime are both UTC (verified 'show timezone' = UTC). Setting TZ to Asia/Kolkata, the natu | Select the column as text in the query ('v.next_visit_recommendation::text') and pass the string straight through — syncPlannedFollowUpVisit already validates it against a YYYY-MM- | S |
| 92 | LOW | CONFIRMED | GOVERNANCE | app/actions/risansi-visits.ts:770 | Debug logging left in the production submit path; the file also uses console.error as its only error channel (lines 238, 363, 839, 896) with no error-monitoring integration anywhere in the repo. | Every visit submission writes a debug line to Vercel logs, and genuine swallowed failures (the follow-up-visit and TSM-notification catches) | Delete the console.log, and route the caught errors to an error reporter (Sentry or equivalent) so the swallowed notification and follow-up failures raise an alert. | S |
| 93 | LOW | CONFIRMED | correctness | app/actions/risansi.ts:186 | opportunityColumns caches an EMPTY set forever after one transient failure (empty is then treated as 'write all candidate columns'), and the information_schema query is not schema-qualified, so a same-named tab | One hiccup at cold start makes every subsequent insert/update attempt all candidate columns for the life of the lambda; a genuinely missing  | Don't cache on error ('if (rows.length) _oppColumns = ...' / leave null so it retries), and add 'AND table_schema = current_schema()'. | S |
| 94 | LOW | CONFIRMED | correctness | app/actions/risansi.ts:257 | The clear-primary UPDATE and the following INSERT/UPDATE in addContact, updateContact and saveContacts are separate non-transactional statements. | Two concurrent saves can interleave to leave a client with two primary contacts or none; there is also no DB-level partial unique index enfo | Wrap clear+write in a transaction, and add 'CREATE UNIQUE INDEX ... ON contacts(client_id) WHERE is_primary' as the real invariant. | S |
| 95 | LOW | CONFIRMED | correctness | app/actions/risansi.ts:1452 | addSalesOrder validates SO number/value but not the date format, while the sibling addPurchaseOrder enforces a YYYY-MM-DD regex (line 1532) and a magnitude sanity cap (line 1536); the SO path has neither. | An unparseable date reaches Postgres and surfaces as the opaque 'Server Components render' error instead of the friendly message; an SO valu | Copy the PO date regex and the 'cr >= 9_999_999' sanity check into addSalesOrder (and into parseSalesOrdersJson for the Won-transition path). | S |
| 96 | LOW | CONFIRMED | data | app/actions/risansi.ts:1338 | Rupee→crore conversions are done in JS double arithmetic throughout (lines 984, 1162, 1338, 1497, 1809) before landing in numeric columns. | IEEE-754 representation of e.g. 1,23,45,678/1e7 introduces tiny binary-fraction residue that numeric then stores faithfully; sums across hun | Pass the rupee value as a parameter and convert in SQL ('$n::numeric / 10000000'), as syncOfferRevisions already does at line 91. | S |
| 97 | LOW | CONFIRMED | performance | app/actions/risansi.ts:1897 | addClient's lead path loads every client code in the table into memory to test LEAD_ code uniqueness (~1400+ clients after the imports, growing). | Full-table scan and payload on every lead creation; the uniqueness test only needs candidate lookups. | Loop uniqueLeadCode's candidates against 'SELECT 1 FROM clients WHERE UPPER(code) = $1' (or fetch only 'WHERE code LIKE 'LEAD\_%''), keeping the same collision callback. | S |
| 98 | LOW | CONFIRMED | performance | app/actions/risansi.ts:1314 | updateOpportunity re-SELECTs the same opportunity row it already fetched at line 1253 ('cur[0]' holds stage, rep_id AND client_id). | A wasted round-trip per edit plus a second correlated-subquery lookup; purely redundant. | Reuse 'cur[0].rep_id' / 'cur[0].client_id' in the rep-preservation block and delete the second query. | S |
| 99 | LOW | CONFIRMED | simplificatio | app/actions/risansi.ts:102 | A local requireSession (redirects, returns session.user) shadows lib/risansi-auth's exported requireSession (throws, returns session) — same name, different contract; several actions additionally hand-roll the  | Two behaviors under one name invite the wrong import; a redirect() inside a programmatically-invoked server action surfaces as an opaque NEX | Keep one helper (in lib/risansi-auth), give it one behavior (throw 'Unauthorized'), and have all actions use it. | S |
| 100 | LOW | CONFIRMED | simplificatio | app/actions/risansi.ts:563 | client_status_log insert failures at lines 563 and 2039 are logged with the copy-pasted message 'opportunity_stage_log insert failed'. | Misdirects debugging: a status-log failure would be hunted in the wrong table. | Correct both messages to 'client_status_log insert failed'. | S |
| 101 | LOW | CONFIRMED | CORRECTNESS | app/actions/sysadmin.ts:258 | mapClients loops one UPDATE plus one audit insert per client with no transaction and no bound on clientIds.length. | A bulk re-map that fails partway leaves some clients on the new tour and some on the old, with no record of where the boundary fell — and to | Do it in one statement — 'UPDATE clients SET tour_id = $1 WHERE id = ANY($2::int[])' — inside a transaction with a single summary audit row. | S |
| 102 | LOW | CONFIRMED | performance | app/api/auth/[...nextauth]/route.ts:71 | The jwt callback fires a users lookup on every getServerSession call — every server-rendered page and every server action. The condition 'user // token.email' is always true once signed in, so there is no fast  | One extra Postgres round-trip per request on serverless; it buys live role revocation, but unconditionally. (users_email_lower_key does cove | Cache the lookup in the token: store 'token.checkedAt' and re-query only when older than ~60s, otherwise reuse the existing risansiAccess/role/repId values. | S |
| 103 | LOW | CONFIRMED | security | app/api/auth/change-password/route.ts:20 | The only password rule is length >= 8. Nothing rejects reusing the current password, nothing caps length (bcryptjs silently truncates past 72 bytes), and there is no breach/common-password check. | A user can 'change' to the same password after a forced reset, and a long passphrase is silently shortened to 72 bytes. | Reject when 'await bcrypt.compare(newPw, hash)' is true ('New password must differ from the current one'), cap length at 72, and mirror the same rules in signup/submit. | S |
| 104 | LOW | CONFIRMED | correctness | app/api/auth/signin/page.tsx:26 | If signIn resolves with neither 'error' nor 'ok' (e.g. result is undefined on an aborted request), neither branch runs, 'setLoading(false)' is never called and no error is shown — the button stays disabled on ' | The user is stuck on a dead form and must reload the page to retry. | Add a trailing 'else { setError('Something went wrong. Please try again.'); setLoading(false); }', or move the reset into a 'finally' and gate the redirect on ok. | S |
| 105 | LOW | CONFIRMED | security | app/api/cron/daily/route.ts:18 | CRON_SECRET is compared with '===', a non-constant-time string comparison. Same code duplicated verbatim in app/api/cron/weekly/route.ts:11. The fail-closed logic itself is correct ('if (!secret) return process | Theoretical remote timing oracle on the secret; low practical risk over the network, but trivially avoidable. | Extract one shared 'lib/cron-auth.ts' and compare with 'crypto.timingSafeEqual(Buffer.from(header), Buffer.from(expected))' after a length check. | S |
| 106 | LOW | CONFIRMED | security | app/api/cron/daily/route.ts:21 | Both cron routes expose their side effects (sending email, writing tasks.last_reminded_at and notification_runs) over GET. | GET is assumed safe by proxies, prefetchers, and retry logic; an accidental replay re-runs the reminder sweeps, which are guarded only by pe | Export POST instead of GET, or keep GET but wrap the two reminder sweeps in a 'claimRun('action_reminders', 'CURRENT_DATE')' guard like the escalation already uses. | S |
| 107 | LOW | CONFIRMED | simplificatio | app/api/debug-db/route.ts:22 | Dead masking code: replacing every character with '*' and then slicing yields the constant '********'. The adjacent line still ships 'db: process.env.RISANSI_DB_NAME' unmasked. The catch also uses 'err: any'. | Pointless code that reads as if it protects something while the real identifier next to it is exposed in clear. | Delete the whole route (see the CRITICAL finding). If it survives, drop both env fields and type the catch as 'unknown'. | S |
| 108 | LOW | CONFIRMED | COST | app/api/risansi/activity/route.ts:24 | ActivityTracker (components/risansi/ActivityTracker.tsx:6-7) flushes every 30s per open tab plus on route change and pagehide, and each POST also triggers a getServerSession that re-queries users. page_activity | Two DB round-trips per user per 30 seconds forever, into a table nothing prunes, on a hosted Postgres where storage and connections are the  | Raise FLUSH_MS to 120s and add to the daily cron: 'DELETE FROM page_activity WHERE occurred_at < now() - interval '180 days'', keeping a rolled-up daily aggregate if history is nee | S |
| 109 | LOW | CONFIRMED | SECURITY | app/api/risansi/client-owners/route.ts:24 | Authenticated but no per-client authorisation: any signed-in user can walk clientId=1..N and read the rep ids on every client's tour, including clients outside their own scope. | Same IDOR class as the logged contact IDOR. Discloses the full client-to-rep org mapping to any rep, which is what the tour-based visibility | After parsing clientId: 'if (!(await canViewClient(await getCurrentUser(), clientId))) return NextResponse.json({ owner_ids: [] }, { status: 403 });'. | S |
| 110 | LOW | CONFIRMED | CORRECTNESS | app/api/risansi/clients/export/route.ts:127 | Both export routes (also opportunities/export/route.ts:254) stamp the filename and the on-sheet subtitle with the UTC date. Vercel runs UTC, users are IST (+5:30). | An export taken between 00:00 and 05:30 IST is labelled with the previous day, so two files from the same working session collide or mis-sor | 'new Intl.DateTimeFormat('en-CA',{timeZone:'Asia/Kolkata'}).format(new Date())' returns YYYY-MM-DD in the business timezone. | S |
| 111 | LOW | CONFIRMED | SIMPLIFICATIO | app/api/risansi/competitors/route.ts:10 | A bare catch retries the query without the is_active predicate to work around a schema uncertainty, then a second bare catch returns []. The column does exist today. | Any real failure (pool exhausted, permission change) is misdiagnosed as 'column missing' and answered with the full unfiltered competitor li | Delete the fallback and both bare catches, keep the single is_active query, and log the error before returning 500. | S |
| 112 | LOW | CONFIRMED | CORRECTNESS | app/api/risansi/validate-revenue-codes/route.ts:32 | The error path returns a success-shaped body. The caller (components/risansi/RevenueUploadBox.tsx:145) never checks res.ok, so on a 500 every code slips past notFound and reaches 'const db = found[row.code]' th | On a bulk revenue import the user sees 'Failed to parse file: Cannot read properties of undefined' for what is actually a database outage. | Return '{ error: 'Validation service unavailable' }' with the 500, and have the three upload boxes check res.ok and show a distinct retry message. | S |
| 113 | LOW | CONFIRMED | SECURITY | app/api/risansi/visit-photo/[photoId]/route.ts:52 | This route and complaint-photo/[photoId]/route.ts:31 echo the stored mime with no X-Content-Type-Options: nosniff, and both upload paths validate only the client-declared file.type against ALLOWED_MIME - no mag | The allow-list keeps Content-Type to raster + heic so modern browsers will not sniff to HTML, but the bugs screenshot route sets nosniff and | Add ''X-Content-Type-Options': 'nosniff'' to both responses, and check leading bytes (FFD8FF / 89504E47 / RIFF..WEBP / ftypheic) against the declared type before INSERT. | S |
| 114 | LOW | CONFIRMED | SIMPLIFICATIO | app/risansi/_components/RisansiSidebar.tsx:36 | This 190-line component is imported by nobody — both layouts render 'Sidebar' from '@/components/risansi', and the only '_components' imports elsewhere resolve to admin/_components/AccessDenied. Stale duplicate | Dead code duplicating nav config drifts out of sync with the real Sidebar and misleads maintainers. | Delete app/risansi/_components/RisansiSidebar.tsx. | S |
| 115 | LOW | CONFIRMED | PERFORMANCE | app/risansi/admin/pumps/page.tsx:84 | 'SELECT *' on pump_upload_log (and the identical 'SELECT * FROM revenue_upload_log' in revenue/page.tsx line 98) fetches every column though the rendered table uses only the fixed LogRow subset. | Over-fetch couples the page to schema changes and ships unused columns on an admin table. | List the columns actually consumed by LogRow instead of '*'. | S |
| 116 | LOW | LIKELY | UI/UX | app/risansi/admin/settings/SettingsForm.tsx:52 | The number input hardcodes light hex (bg #F8FAFC, text #0D1B2A, border #CBD5E1) instead of CSS theme tokens. Same literal-hex input pattern in UsdRateForm.tsx (INP), tours/ToursClient.tsx (INP) and unassigned/U | Project rule (CLAUDE.md tokens / dark-mode memory) mandates tokens; these inputs stay light in dark mode, clashing with the themed surface. | Use the token set (e.g. var(--bg-paper), var(--fg), var(--line-strong)) instead of literal hex on these inputs. | S |
| 117 | LOW | CONFIRMED | SIMPLIFICATIO | app/risansi/admin/settings/UsdRateForm.tsx:7 | UsdRateForm.tsx and SettingsForm.tsx are near-identical: same useState/useTransition state machine, layout and INP/BTN style constants; they differ only in label, unit text and which action they call. | Two copies drift independently — a validation or a11y fix applied to one silently misses the other. | Extract one NumberSettingForm({ label, unit, current, action }) and render both settings from it. | S |
| 118 | LOW | CONFIRMED | UI/UX | app/risansi/clients/FilterBar.tsx:79 | The search input has only a placeholder (no <label>/aria-label), and the sugar segmented-control buttons convey selection purely by color with no aria-pressed. | Screen-reader users get no accessible name for the search field and no announced state for the active sugar filter. | Add aria-label='Search clients' to the input; add aria-pressed={sugar === opt.value} to each toggle button. | S |
| 119 | LOW | CONFIRMED | CORRECTNESS | app/risansi/clients/[id]/page.tsx:567 | Order-in-hand per Won opp is computed as a JS float subtraction of crore values (numeric(14,7) cast to float8/text), and any positive result (>0) marks the Won as still open and increments openWonCount. | Sub-rupee float residuals (e.g. 2.1 − 0.7−0.7−0.7 ≈ 4e-16 Cr) can classify a fully-fulfilled Won as 'Open · ₹0 in hand' and inflate the 'N w | Round to paise before the >0 test: 'const inHand = Math.round((finalCr - soSum) * 1e7) / 1e7; return Math.max(0, inHand);' and treat /inHand/ < 1 rupee as zero. | S |
| 120 | LOW | CONFIRMED | UI/UX | app/risansi/clients/[id]/page.tsx:698 | Hardcoded light-theme hex colors on the Export-PDF button (698-700), visit-row PDF button (1137-1139), contact avatar (#0A3D8F/#fff, 1230-1231), and both loading.tsx topbar placeholders (background:'#fff'). | Project rule (CLAUDE.md color-token rule + dark-mode memory) is to use CSS var tokens, never raw hex; these fixed light colors render wrong  | Replace with tokens: e.g. background var(--accent-soft)/var(--bg-elev), color var(--accent), border var(--line-strong); topbar placeholder background var(--bg-paper). | S |
| 121 | LOW | CONFIRMED | SIMPLIFICATIO | app/risansi/clients/[id]/page.tsx:1661 | Style constants TH (1661) and TD (1667) are defined but never referenced anywhere in the file (only REV_TH is used). | Dead code; misleads readers into thinking the detail page has a shared table style. | Delete the unused TH and TD constants. | S |
| 122 | LOW | CONFIRMED | SECURITY | app/risansi/clients/page.tsx:216 | The industry (216), zone (224) and tier (233) filter-option queries are NOT visibility-scoped, unlike the clientTypes/rep/revenue/visit option queries which correctly apply clientVisibilitySql(user,'c'). | A rep sees the full set of distinct industries/zones/tiers across ALL clients including those outside their tour. Low sensitivity (aggregate | Add the same 'AND (${clientVisibilitySql(user,'c')})' guard (with the null check) to the industry/zone/tier DISTINCT queries. | S |
| 123 | LOW | CONFIRMED | SIMPLIFICATIO | app/risansi/clients/page.tsx:379 | The export-href param assembly (379-392) re-implements the same filter-to-querystring mapping already encoded in buildUrl (338-363). | Two copies of the filter list must be kept in sync; a new filter added to one and not the other silently drops from the export or the pagina | Derive exportHref from a shared param-map (or reuse buildUrl's base object) so filters are listed once. | S |
| 124 | LOW | LIKELY | CORRECTNESS | app/risansi/compete/page.tsx:36 | fmtD parses a date-only 'YYYY-MM-DD' string (visit_date cast ::text) with new Date(), which is interpreted as UTC midnight, then formats with toLocaleDateString in the server timezone. | On a server TZ behind UTC the displayed day shifts back one; pipeline/stage pages keep dates as ::text to avoid this. Safe on a UTC Vercel r | Format the date string directly (split on '-') or parse as UTC and format with timeZone:'UTC', matching how the other pages treat date-only values. | S |
| 125 | LOW | CONFIRMED | SECURITY | app/risansi/compete/page.tsx:255 | The zoneOptions (p.255-263) and repOptions (p.266-271) filter dropdowns are not visibility-scoped, unlike every data query on the page — repOptions returns every active user name, zoneOptions every zone regardl | A rep sees the full list of zones and all active user names even for clients/reps outside their tour scope. Low sensitivity (filter option l | Scope both option queries to the caller's visible clients (join clients + clientVisibilitySql), as the pipeline page does for its industry/client-type options. | S |
| 126 | LOW | CONFIRMED | SECURITY | app/risansi/field/page.tsx:360 | Query 4's comment says 'admin only' but the guard is 'if (isRep) return []', so managers (isRep=false) also get the full active rep/manager roster. That roster feeds AssignVisitDrawer/AssignVisitButton, exposin | Over-exposure of the full user roster to managers, contrary to the tour-scoped model; the assign controls then list reps a manager shouldn't | Restrict the roster to getManagerAssignableReps(uid) for managers (admin still gets all), and verify the assign server action validates the target rep against the manager's assigna | S |
| 127 | LOW | CONFIRMED | GOVERNANCE | app/risansi/field/page.tsx:472 | Leftover DEBUG console.log block (lines 472-473 and 498-502) runs unconditionally on every field-page server render, dumping role, repId, week/month bounds, the fetched reps list, and every fetched visit's id/r | Console-only debug logging on a hot path: log-volume/cost noise and record identifiers (rep names, visit ids) written to logs on every reque | Delete the DEBUG console.log statements (or gate behind an explicit debug env flag). | S |
| 128 | LOW | CONFIRMED | UI/UX | app/risansi/field/page.tsx:1127 | OverdueSortTH builds its sort link from scratch, omitting the active zone/tour/rep filters that tabHref() otherwise preserves. Clicking a column header to re-sort silently resets all active filters on the Overd | Sorting and filtering are competing rather than composable; users lose their filter context on every sort click. | Build the sort href through the same filter-preserving helper as tabHref (append zone/tour/rep and keep tab=overdue), or reuse tabHref with sort/dir extras. | S |
| 129 | LOW | CONFIRMED | UI/UX | app/risansi/layout.tsx:97 | The layout's outer flex container (line 97) and <main> (line 112) hardcode light hex #F4F6FB instead of var(--bg). The app supports a dark theme (:root.dark flips --bg to #0D1B2E). admin/layout.tsx has identica | In dark mode the main scroll surface stays light behind page content, producing a visible light band on the core app shell. | Replace the inline '#F4F6FB' backgrounds in both layouts with 'var(--bg)'. | S |
| 130 | LOW | CONFIRMED | SIMPLIFICATIO | app/risansi/layout.tsx:69 | Both risansi/layout.tsx (line 69) and admin/layout.tsx (line 66) call 'await headers()' and run the isMobileUA regex to compute '_isMobile', which is never used. | Dead computation reads request headers and runs a regex on every risansi/admin render for no effect. | Remove the '_isMobile' line (and the unused ua/headers read if nothing else needs it) in both layouts. | S |
| 131 | LOW | CONFIRMED | UI/UX | app/risansi/mobile/visit/new/NewVisitClient.tsx:123 | The client-search label (and the 'Visit Purpose' label at line 202) are not programmatically associated with their controls - no htmlFor/id, and the input has no aria-label. | Screen-reader users on the mobile check-in flow get an unlabelled search field and an unlabelled purpose group. | Give the input an id and the label htmlFor (or wrap the input in the label / add aria-label); group the purpose buttons in a fieldset with a legend or role=radiogroup + aria-label. | S |
| 132 | LOW | CONFIRMED | CORRECTNESS | app/risansi/page.tsx:139 | Rep-dashboard KPIs are labelled/commented as personal ('My visits', 'owner visibility on v.rep_id', 'owner visibility on o.rep_id' at lines 139/160/181) but the predicates use clientScopeSql on v.client_id / o. | Stale comments plus a misleading label: 'My Visits This Week / Last 7 days' and 'My Recent Visits' actually show co-reps' tour activity, dou | Either relabel to 'Tour' wording and fix the comments to say tour-scoped, or add an explicit v.rep_id/o.rep_id = repId filter if the intent is truly personal. | S |
| 133 | LOW | CONFIRMED | SIMPLIFICATIO | app/risansi/page.tsx:1368 | GhostBtn is defined but never referenced anywhere in the file (grep confirms only the definition). | Dead code that adds maintenance surface and confusion. | Delete the GhostBtn function. | S |
| 134 | LOW | CONFIRMED | SIMPLIFICATIO | app/risansi/pipeline/page.tsx:149 | Both branches of the orderDir ternary return 'DESC', so ascending order is impossible. More broadly SORT_MAP/sortKey/sortCol are vestigial: ActiveOppsTable sorts entirely client-side (its own SORT_ACCESSOR/sort | Dead/misleading code — the server ORDER BY only ever fixes a value-DESC default that the client table immediately re-sorts, and a reader wou | Either wire real server-side sort links to ?sort/?dir (and make orderDir honor sp.dir), or drop SORT_MAP/sortKey/sortCol/orderDir and hard-code ORDER BY o.value_cr DESC. | S |
| 135 | LOW | NEEDS-CHECK | SECURITY | app/risansi/pipeline/stage/[stage]/page.tsx:352 | quotation_link comes straight from the DB (historically a free-text 'Quotation Link' field) and is rendered as an anchor href with no scheme validation. | A stored javascript:/data: or hostile URL entered by an authenticated staff member could execute when another clicks it. Low: internal porta | Validate the scheme before rendering (allow only http/https, else render as plain text), or since it is now an internal upload path, restrict to the known /api path prefix. | S |
| 136 | LOW | CONFIRMED | SECURITY | app/risansi/registry/page.tsx:71 | The Responsible-filter option list is fetched unscoped ('small, so fetched unscoped'), so the dropdown shows the names of every rep who has an assigned task to any non-admin user viewing the Action Registry. | Minor over-exposure: rep names leak to reps/managers regardless of tour scope, even though the task rows themselves are correctly scoped by  | Scope the responsible-options query to the caller's visible tasks for non-admins (join through repVisibilitySql conditions), or accept and document that rep names are non-sensitive | S |
| 137 | LOW | CONFIRMED | SIMPLIFICATIO | app/risansi/revenue/page.tsx:70 | withRep() replaces a legacy ' AND c.primary_rep_id = $REP' placeholder that no longer appears in any query string (every query already inlines ${repCond} directly), so the regex matches nothing and the wrapper  | Dead indirection wrapped around all 9 queries obscures that repCond is already interpolated and invites confusion on the next edit. | Delete withRep() and call risansiPool.query with the already-interpolated SQL and base params directly. | S |
| 138 | LOW | CONFIRMED | CORRECTNESS | components/risansi/ActivitiesTab.tsx:84 | Overdue detection parses the date-only due_date as UTC midnight and compares to new Date() (now). For IST users a task due TODAY reads as overdue for the whole day. Same bug at ActivitiesTab.tsx:141 and Complai | Today's actions/complaints are wrongly flagged overdue, inflating the overdue KPI counts. | Compute a local todayStr (YYYY-MM-DD) and compare strings (due_date < todayStr), as ActionQueueRow.tsx:34-35 does. | S |
| 139 | LOW | CONFIRMED | CORRECTNESS | components/risansi/AssignVisitDrawer.tsx:273 | nextWorkday() increments a local date then serializes with toISOString().slice(0,10) (UTC). For IST users before ~05:30 local the UTC date is the previous day, so the prefilled visit date can land one day earli | Default planned-visit date is occasionally off by a day; user-correctable but misleading. | Format from local parts: '${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}-${String(d.getDate()).padStart(2,'0')}'. | S |
| 140 | LOW | CONFIRMED | GOVERNANCE | components/risansi/AssignVisitDrawer.tsx:288 | A left-in debug block dumps client_id, rep_id, the full selectedClient object, prefilledClient, role and every FormData entry to the browser console on every visit submit in production. | Ships internal identifiers/PII to the console as noise and signals unfinished code; should not be in production. | Delete the debug block (lines 288-300) including the fd.entries() loop. | S |
| 141 | LOW | CONFIRMED | UI/UX | components/risansi/DeletePumpUploadButton.tsx:21 | On a failed delete the confirm UI just collapses back to the plain Delete button with no error message and no refresh; the row remains and the user has no feedback that anything failed. DeleteUploadButton.tsx l | Silent failure — the admin cannot tell a failed delete from a no-op and may assume the upload was removed when it was not. | Add an error state and render a short message on catch (as EditContactButton/EditVisitButton do) instead of silently resetting to the idle button. | S |
| 142 | LOW | LIKELY | CORRECTNESS | components/risansi/Donut.tsx:33 | A donut with a single slice at 100% produces an arc whose start and end points are identical (startDeg -90, endDeg 270 → same x/y). Per SVG, an arc between coincident endpoints renders nothing, so the ring disa | When data collapses to a single category (a common real case, e.g. all clients one status) the chart silently renders blank instead of a ful | Special-case near-360° total: when a single slice covers ~100% render a full <circle> stroke, or split any ≥360° slice into two arcs. | S |
| 143 | LOW | CONFIRMED | CORRECTNESS | components/risansi/ExportPdfButton.tsx:6 | The export filename date is built from toISOString() (UTC). Between 00:00 and 05:30 IST the UTC date is still the previous calendar day, so the exported PDF is stamped with yesterday's date. | Cosmetic but the flagged toISOString day-shift pattern; a PDF exported early morning IST carries a misleading date in its filename. | Build the date in local time, e.g. new Date().toLocaleDateString('en-CA') or from getFullYear/getMonth/getDate. | S |
| 144 | LOW | CONFIRMED | SIMPLIFICATIO | components/risansi/FYSelector.tsx:13 | FYSelector is exported but imported nowhere in the repo (grep of the whole tree matches only this file; not re-exported from index.ts) — dead code. Its FY_OPTIONS are hardcoded with the newest year 25-26 and de | Dead code rots and misleads; the stale hardcoded list means the control silently locks users out of the current year if ever mounted. | Delete FYSelector.tsx, or if intended for future use, derive the FY list from the current date rather than a hardcoded array and add the 26-27 option. | S |
| 145 | LOW | CONFIRMED | performance | components/risansi/NewOpportunityModal.tsx:113 | searchClients (L70) fires a /api/risansi/clients-search fetch on every keystroke once length>=2, with no debounce. The endpoint runs a per-user-visibility ILIKE query with correlated owner-name subqueries per h | A word typed into the picker fires ~1 request per character, multiplying a non-trivial DB query and adding needless load/cost. | Debounce the fetch ~250ms (or use a timeout / AbortController) so only the settled query hits the server. | S |
| 146 | LOW | CONFIRMED | correctness | components/risansi/OfferRevisionsField.tsx:19 | Default 'Revised On' date uses toISOString() (UTC). In IST/GST between local midnight and ~05:30 the ISO date is the previous day, so a new revision defaults to yesterday. | revised_on is a persisted business date in the offer-revision history; the default can silently be a day early. | Build the date from local components like PlannedVisitsExport does: '${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}-${String(d.getDate()).padStart(2,'0')}'. | S |
| 147 | LOW | CONFIRMED | correctness | components/risansi/OppCompletionModal.tsx:93 | The Won 'Final Value (₹)' uses <input type="number"> instead of the shared MoneyInput. Comma-grouped amounts can't be typed and a pasted '25,00,000' is rejected/blanked, with no ₹ echo. PurchaseOrderManager L10 | Inconsistent entry and paste-loss risk on a critical closing amount; required-ness makes it fail visibly, but it diverges from every other m | Replace with <MoneyInput name='final_value_inr' value={finalInr} onChange={setFinalInr} usdRate=… required/>; same for PurchaseOrderManager's Value field. | S |
| 148 | LOW | CONFIRMED | correctness | components/risansi/OutstandingUploadBox.tsx:38 | The 'Outstanding as of' date defaults via toISOString() (UTC), so in +TZ early-morning hours it shows the previous day. PlannedVisitsExport in the same batch explicitly avoids this bug. | A wrong as-of default is written to outstanding_as_of and the upload log if the admin doesn't notice; inconsistent with the codebase's own l | Compute the default from local Y/M/D components instead of toISOString().slice(0,10). | S |
| 149 | LOW | CONFIRMED | correctness | components/risansi/QuotedDetailsModal.tsx:265 | offerInr is the raw MoneyInput string that may contain Indian commas (e.g. '8,50,000'); parseFloat stops at the first comma and yields 8, so the 'vs original %' delta base is wrong. NewOpportunityModal uses par | The revision delta shown to reps is computed against a garbage base whenever the Total Offer is typed with grouping commas. | Use parseMoneyInput(offerInr) instead of parseFloat(offerInr). | S |
| 150 | LOW | CONFIRMED | ui-ux | components/risansi/RevenueTopClients.tsx:78 | Sortable column headers are <th onClick> with no button, tabIndex, keyboard handler, or aria-sort, so sorting is mouse-only and unannounced to assistive tech. The th() helper on L46 also accepts a 'label' param | Keyboard and screen-reader users cannot change the sort and get no sort-state feedback. | Render the header label as a <button> (or add role/tabIndex+onKeyDown) and set aria-sort on the active column; drop the unused label param. | S |
| 151 | LOW | CONFIRMED | UI/UX | components/risansi/SalesOrderList.tsx:85 | The 'covered' SO-coverage banner uses literal light-only hex ('#D1FAE5' bg, '#065F46' text), while its sibling SalesOrderManager renders the identical Open/Closed state with tokens ('var(--pos-soft)'/'var(--pos | Violates the project color-token rule (never hardcode, theme flips via CSS vars) and is inconsistent with the sibling component doing the sa | Swap to the pos tokens used by SalesOrderManager: background 'var(--pos-soft)', color 'var(--pos-strong)', border derived likewise. | S |
| 152 | LOW | CONFIRMED | SIMPLIFICATIO | components/risansi/SortableTH.tsx:27 | SortableTH.handleClick re-implements byte-for-byte the same sort-navigation logic that already lives in the useSortableTable hook (handleSort). Two copies of the same URL-param toggle drift independently. | Maintenance hazard: a fix to sort behaviour (e.g. preserving a new param) must be made in two places or one silently diverges. | Have SortableTH consume useSortableTable (handleSort/isActive/sortIcon) instead of duplicating the body, or extract a shared pushSort() helper both call. | S |
| 153 | LOW | CONFIRMED | PERFORMANCE | components/risansi/Topbar.tsx:13 | LiveIndicator ticks a 1000ms interval that re-renders the Topbar every second, for the life of every page, purely to age a 'synced Ns ago' label. | A forever 1Hz re-render on every screen is needless work/battery on the field reps' phones, for a label that only needs coarse updates. | Back off the cadence (e.g. update every 15–30s, or switch to minute granularity after the first minute), or stop after a few minutes — the exact second is not meaningful. | S |
| 154 | LOW | LIKELY | CORRECTNESS | components/risansi/UsersManager.tsx:198 | The inline Deactivate/Reactivate button resends the entire user row from the (possibly stale) client-side snapshot; updateRep applies every submitted column. A concurrent edit by another admin to email/role/tar | Two sysadmins on the same list can clobber each other's edits — a classic last-write-wins lost update on account fields, with no optimistic- | Send only the field being changed: 'updateRep(user.id, fd({ is_active: user.is_active ? 'false' : 'true' }))' (updateRep already updates only submitted columns), so the toggle can' | S |
| 155 | LOW | CONFIRMED | CORRECTNESS | components/risansi/VisitPhotos.tsx:110 | A network side-effect (DELETE fetch) is fired from inside a setState updater. Updaters must be pure; React may invoke them more than once (StrictMode/concurrent rendering), issuing duplicate DELETE requests. | Harmless today because the DELETE is idempotent (WHERE id=$1), but it is an anti-pattern that will bite if the endpoint ever becomes non-ide | Do the optimistic update and the fetch outside the updater: capture 'const prev = photos; setPhotos(photos.filter(...)); fetch(...).catch(() => setPhotos(prev))'. | S |
| 156 | LOW | CONFIRMED | simplificatio | components/ui/select.tsx:82 | The SelectContent Viewport passes 'position === "popper" && ""' as a second argument to cn(); it evaluates to either the empty string or false, so it contributes nothing to the className. | Dead/no-op code that reads as if it toggles a class but does nothing, adding noise and future-maintenance confusion. | Delete the 'position === "popper" && ""' argument (lines 82-83), leaving only the data-[position=popper] class string in the cn() call. | S |
| 157 | LOW | CONFIRMED | security | lib/risansi-auth.ts:158 | complaintVisibilitySql is the one place in the auth core that interpolates non-numeric data into SQL, escaped only by doubling single quotes. | Safe as deployed (standard_conforming_strings is 'on', verified live; the email comes from the users table, not input) but it is a hand-roll | Return '{ sql, params }' from the builder like buildClientFilter does, or drop the literal: 'lower(cm.created_by) = (SELECT lower(email) FROM users WHERE id = <uid>)'. | S |
| 158 | LOW | CONFIRMED | simplificatio | lib/risansi-auth.ts:25 | requireSession (line 25) and getRepTours (line 32) are exported but imported nowhere; app/actions/risansi.ts:102 defines a second, divergent requireSession that redirects to /api/auth/signin instead of throwing | Two names for the same gate with different failure semantics invites callers to pick the weaker one, and dead exports obscure which auth hel | Delete getRepTours; delete the local copy in app/actions/risansi.ts and export one 'requireSession()' from lib/risansi-auth.ts that checks session.user.email plus risansiAccess === | S |
| 159 | LOW | CONFIRMED | correctness | lib/risansi-client-filter.ts:70 | The free-text search term is parameterised (no injection) but its LIKE metacharacters are not escaped, so '%' and '_' typed in the search box act as wildcards. | A name fragment containing '%' or '_' returns unrelated rows; a bare '%' matches all 2,676 clients, and the same clause drives the Excel exp | 'const esc = q_str.replace(/[\\%_]/g, c => '\\' + c);' then append 'ESCAPE '\\'' to each ILIKE in the clause. | S |
| 160 | LOW | CONFIRMED | ui-ux | lib/risansi-client-status.ts:36 | Line 31 claims "Chosen so the text meets WCAG AA on its tint", but #6B7280 on #F3F4F6 measures 4.39:1, below the 4.5:1 AA threshold for normal text. Used by both INACTIVE and DUPLICATE. | Status badges render at small sizes so AA normal-text applies; the other five pairs all pass (4.51 to 5.30:1), making this an outlier rather | Darken the foreground to #4B5563 (about 6.3:1 on #F3F4F6) for the INACTIVE and DUPLICATE entries. | S |
| 161 | LOW | LIKELY | correctness | lib/risansi-email.ts:58 | prettyDate parses a bare 'YYYY-MM-DD' as UTC midnight and then formats it with toLocaleDateString('en-IN') in the host timezone. | Correct on Vercel (UTC), so not broken today; on any host west of UTC every emailed due date, visit date and target date renders one day ear | Format from the string parts when the input matches /^\d{4}-\d{2}-\d{2}$/, or pass '{ timeZone: 'Asia/Kolkata' }' to toLocaleDateString. | S |
| 162 | LOW | CONFIRMED | correctness | lib/risansi-lead-code.ts:40 | uniqueLeadCode's uniqueness check is a synchronous callback over a pre-read snapshot, with no atomicity against the insert that follows. | clients.code is UNIQUE (clients_code_key, verified), so concurrent lead creation surfaces a raw 23505 to the second user instead of picking  | Wrap the insert in a retry: catch 'err.code === '23505'' on clients_code_key, re-run uniqueLeadCode with the freshly taken code added, retry up to 3 times. | S |
| 163 | LOW | CONFIRMED | CORRECTNESS | lib/risansi-notify.ts:44 | claimRun() fails open (returns true on any DB error) and is called BEFORE the send in runAdminOverdueEscalation/runWeeklyManagerDigest. A transient error on notification_runs makes the once-per-day/week dedup b | The whole point of claimRun is to stop repeat cron fires re-blasting admins/managers; fail-open re-sends on transient errors, while claim-be | Distinguish 'relation does not exist' (42P01 → return true) from other errors (return false to skip). For digests, claim per-recipient or stamp only after a successful send. | S |
| 164 | LOW | LIKELY | CORRECTNESS | lib/risansi-utils.ts:99 | formatLastVisit/formatLastVisitShort compute 'days ago' using the server's LOCAL midnight. On a UTC Vercel server, a date-only string ('YYYY-MM-DD') is parsed as UTC midnight and the day boundary is UTC, so for | Cosmetic but visible off-by-one: a visit logged 'today' can render as '1 day ago', and overdue thresholds tip a day early during that IST wi | Compute both dates in IST (add 5.5h before setHours, as getGreeting does) or compare on date-parts derived in Asia/Kolkata rather than server-local time. | S |
| 165 | LOW | CONFIRMED | SECURITY | lib/risansi-visit-edit-window.ts:35 | VISIT_EDITABLE_SQL interpolates its 'col' argument directly into a SQL predicate. All current callers pass a constant, so it is safe today, but the signature invites a future caller to pass a request-derived co | Latent injection sink: the function name/shape gives no hint that 'col' is inlined unescaped into SQL. | Drop the parameter (hardcode submitted_at) or allowlist col against a known set of column names before interpolating. | S |
| 166 | LOW | CONFIRMED | DATA LAYER | app/actions/admin.ts:67 | assigned_by is character varying and receives an email here, while sysadmin.ts:181 writes '(SELECT id FROM users WHERE lower(email) = lower($4))' — a users.id — into the same column. | Verified in the data: assigned_by currently holds '26' (253 rows), '21' (2 rows) and 'import:tour-template' (49 rows). The column has three  | Pick one representation — a nullable assigned_by_user_id integer FK to users(id) — backfill the numeric strings, resolve the emails, and update both writers. | M |
| 167 | LOW | CONFIRMED | simplificatio | app/actions/risansi.ts:1155 | The ItemInput interface, the iStr/iNum/iInt coercers, the items_json parse+filter, and the opportunity_items insert loop are duplicated verbatim between createPipelineOpportunity (lines 938-945, 1085-1097) and  | The file's own quotedItemHasData comment shows why duplication here bites ('the two can't drift') — yet the surrounding item machinery still | Extract 'parseQuotedItems(formData)' and 'replaceOpportunityItems(client, oppId, items)' into lib/ (next to quotedItemHasData's shared logic) and call from both. | M |
| 168 | LOW | CONFIRMED | SECURITY | app/risansi/clients/[id]/page.tsx:183 | The client fetch uses SELECT c.* and the entire row is passed as clientData to ClientActionButtons, a 'use client' component, so every column (weightage_score, constraints_notes, mgmt_intervention, competitors_ | Ships all columns, including ones never rendered, to the client bundle. The viewer already may see this client so it is not a cross-tenant l | Select only the columns the page and the edit drawer actually use, and pass ClientActionButtons a narrowed prop object rather than the whole row. | M |
| 169 | LOW | LIKELY | PERFORMANCE | app/risansi/clients/page.tsx:110 | The main list WHERE applies UPPER(c.status) and a 5-column leading-wildcard ILIKE ('%q%'), and REV_JOIN aggregates all of client_revenue_monthly, re-run across the main+count+tab+stats queries. | UPPER(status) and leading-wildcard ILIKE cannot use a plain b-tree index, forcing sequential scans; fine at a few thousand clients but degra | Store status normalized (or add a functional index on UPPER(status)); for search consider pg_trgm GIN indexes on legal_name/code; verify an index exists on client_revenue_monthly(c | M |
| 170 | LOW | CONFIRMED | CORRECTNESS | app/risansi/revenue/page.tsx:202 | Revenue-by-rep LEFT JOINs tour_assignments, so a client whose tour has N reps contributes its full revenue N times (once per rep); the per-rep totals therefore do not reconcile to the FY total, and vs-target is | Managers reading the table may over-credit reps and misjudge target attainment when tours are co-staffed. | Deliberately document the attribution and label it 'attributed (may overlap)', or attribute to a single primary rep per tour, or split revenue by rep share. | M |
| 171 | LOW | CONFIRMED | PERFORMANCE | app/risansi/visits/coverage/page.tsx:57 | The active-clients query has no LIMIT and runs a correlated string_agg subquery over tour_assignments/users per client row; the full result set is shipped to the CoverageMapSvg client component. | For an admin this scans every active client with a per-row subquery each render; grows linearly with the client base and inflates the client | Replace the correlated subquery with a single LEFT JOIN + GROUP BY (or a lateral), and cap/paginate the pin set if it grows. | M |
| 172 | LOW | CONFIRMED | UI/UX | components/risansi/ActiveFilterBar.tsx:88 | Filter pills hardcode light hex values instead of theme tokens; likewise the Add Rep and Add Tour modals use background:'white' (AddRepButton.tsx:48, AddTourButton.tsx:39), and several buttons hardcode '#0A3D8F | Breaks the project's dark-mode token rule (use CSS var tokens, never hex), giving low-contrast, off-theme surfaces in dark mode. | Swap for tokens: var(--accent-soft)/var(--title)/var(--accent-line) for pills, var(--bg-paper) for modal surfaces, var(--brand-blue) for accents. | M |
| 173 | LOW | CONFIRMED | GOVERNANCE | components/risansi/VisitReportsTab.tsx:7 | Visit report rows are typed as 'Record<string, any>', so the ~40 fields read off 'visit.*' (client_id, submitted_at, gps_within_radius, auto_opp_count, etc.) are entirely unchecked; a renamed/removed aggregate  | This is the app's densest read surface for the visit feed; 'any' removes the one guard that would catch a query/column drift before it ships | Define an explicit interface for the aggregate row shape the page query returns and type 'visits: VisitReportRow[]' against it (numbers-as-strings can be 'string / number'). | M |
| 174 | LOW | CONFIRMED | performance | lib/risansi-action-queue.ts:25 | The row cap is baked into the shared TASK_ORDER constant, so buildTasksQuery cannot paginate; the registry page shows open/overdue counts from buildTasksCountQuery but never says the list was truncated. | 49 tasks today so latent, but past 200 the Action Registry silently stops showing actions while the header count keeps climbing, with no way | Move the cap into TaskQueryOpts ('limit', 'offset'), append 'LIMIT $n OFFSET $n+1' in buildTasksQuery, and render a pager plus "showing 1-200 of N" from the count query. | M |
| 175 | LOW | NEEDS-CHECK | PERFORMANCE | lib/risansi-quotation-parser.ts:153 | rowRe has adjacent lazy quantifiers over overlapping character classes that also include space, separated by \s+. On a long ambiguous run (uppercase/digit/space with no trailing amount) the engine can backtrack | Potential catastrophic backtracking (ReDoS) on adversarial PDF-extracted text. Input is an authenticated rep/manager upload, so worst case i | Anchor rows more tightly (e.g. split lines and match per-line), or bound the class widths; add a length guard on the section body before running the global regex. | M |
| 176 | LOW | CONFIRMED | GOVERNANCE | lib/risansi-sales-orders.ts:25 | Money-parsing and SQL-building helpers (parseSalesOrdersJson, parseOfferRevisionsJson, parseQuotationText, buildOppFilter, summariseStage) have no unit tests — the only *.test.* files are under node_modules — d | Rupee↔crore conversion, per-row validation and pipeline SQL are correctness-critical and silently regressible; console-only logging in cron/ | Add unit tests for the parse/summary helpers (they are already pure). Route caught errors in risansi-notify.ts through an error monitor (Sentry/log drain) instead of console.error  | M |
